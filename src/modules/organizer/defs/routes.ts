import { CrudAppRoutes } from '@common/defs/types';

const prefix = '/organizer/dashboard';
const Routes = {
  Dashboard: prefix,
  // ReadAll: prefix + '/',
  // CreateOne: prefix + '/create',
  // ReadOne: prefix + '/{id}',
  // UpdateOne: prefix + '/{id}/edit',
  // DeleteOn: prefix + '/{id}/delete',
  // CreateOnEvent: prefix + '/create',
  // ReadOneEvent: prefix + '/events/{id}',
  // UpdateOneEvent: prefix + '/events/{id}/edit',
  // DeleteOneEvent: prefix + '/events/{id}/delete',
};

export default Routes;
